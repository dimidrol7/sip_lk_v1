import * as actionTypes from '../actionTypes/SipActionTypes';


export const initialSip = {
};

export const sip = (state = initialSip, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
